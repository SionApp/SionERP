# Copiá este archivo como `keystore.properties` (mismo dir) y completá con tus valores.
# `keystore.properties` está gitignored — nunca se sube al repo. Este .example SÍ se versiona,
# así que NO pongas passwords reales acá.
storeFile=/ruta/absoluta/a/tu/sionerp-release.jks
storePassword=TU_PASSWORD_DEL_KEYSTORE
keyAlias=sionerp
keyPassword=TU_PASSWORD_DE_LA_KEY
