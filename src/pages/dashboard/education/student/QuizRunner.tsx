import { HelpCircle } from 'lucide-react';

import { PlaceholderScreen } from '../PlaceholderScreen';

export default function QuizRunner() {
  return (
    <PlaceholderScreen
      icon={HelpCircle}
      title="El mini quiz está en construcción"
      description="Acá vas a responder las preguntas de la lección antes de avanzar."
    />
  );
}
